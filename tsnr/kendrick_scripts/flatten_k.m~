function f = flatten_k(m)

% function f = flatten(m)
% Kendrick version - appears to be an adaptation of obsolete(?) flatten
% function in matlab toolbox/robust/
% <m> is a matrix
% 
% return as a row vector.
%
% example:
% a = [1 2; 3 4];
% isequal(flatten(a),[1 3 2 4])

f = m(:).';
